---
# GlusterFS Handlers

- name: Restart glusterd
  ansible.builtin.systemd:
    name: glusterd
    state: restarted
    daemon_reload: yes

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
