# Python requirements for CircleCI Kubernetes Ansible project
# Rocky Linux 8 최적화 버전 - 시스템 Ansible 권장 사용법
# 
# 권장 설치 방법:
# 1. sudo dnf install -y ansible (시스템 패키지 - 권장)
# 2. python3.12 -m pip install -r python-requirements.txt --user (Python 라이브러리만)
#
# 주의: Ansible 자체는 시스템 패키지(dnf)로 설치하는 것을 권장합니다.

# Kubernetes Python client for k8s modules
kubernetes>=24.2.0
openshift>=0.13.1

# YAML processing (보통 시스템에 기본 설치됨)
# PyYAML>=6.0

# Jinja2 templating (Ansible과 함께 설치됨)
# Jinja2>=3.1.0

# HTTP requests (for downloading resources)
requests>=2.28.0

# JSON path operations
jsonpath-ng>=1.5.0

# Cryptography for Ansible Vault (보통 시스템에 기본 설치됨)
# cryptography>=3.4.8

# Additional utilities
netaddr>=0.8.0
dnspython>=2.2.0

# Rocky Linux 8 환경 참고사항:
# - Ansible: dnf install -y ansible (9.2.0+)
# - Python: 3.12.10 기본 지원
# - 필수 라이브러리들은 대부분 시스템에 기본 포함됨 