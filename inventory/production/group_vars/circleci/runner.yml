# CircleCI Runner Configuration
---
# Core CircleCI configuration
circleci:
  namespace: "cubrid"
  resource_class: "large"
  token: "{{ vault_circleci_token }}"
  replicas: 1                    # High Stability mode: single agent for maximum stability
  maxConcurrentTasks: 20         # Optimized for 4 core setting

# Resource configuration for job containers
resources:
  job:
    requests:
      cpu: "2"
      memory: "4Gi"
    limits:
      cpu: "32"
      memory: "32Gi"

# Directory paths
paths:
  circleci_config: "/opt/circleci/config"
