ansible==9.13.0
# Needed for community.crypto module
cryptography==45.0.4
# Needed for jinja2 json_query templating
jmespath==1.0.1
# Needed for ansible.utils.ipaddr
netaddr==1.3.0
# Needed for Kubernetes operations
kubernetes>=31.0.0,<32.0.0
# Needed for YAML processing
PyYAML==6.0.2
